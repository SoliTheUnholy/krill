/**
 * Stable public entry point used by the landing page.
 * The implementation lives in `components/order-form/` to keep responsibilities
 * separated and make individual fields, steps, and estimate output reusable.
 */
export { OrderBriefEstimator } from "@/components/order-form/order-form";
